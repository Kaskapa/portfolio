services:
  portfolio-app:
    # Overwrites the production CMD
    command: sh -c "npm install && npm run dev"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true # Essential for hot-reload on many Linux distros
